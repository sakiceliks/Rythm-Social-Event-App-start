import React, { Suspense } from "react";
import { View, Text } from "react-native";

const ProfileScreen = () => {
  return (
    <View style={{ flex: 1 }}>
    <Text style={{color:'white',textAlign:'center'}}>Profile Screen</Text>
    </View>
  );
};

export default ProfileScreen;
